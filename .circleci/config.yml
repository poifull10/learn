version: 2
jobs:
  build:
    docker:
      - image: poifull10/learn_wip

    steps:
      - checkout

      - run:
          name: build_all
          command: |
            git submodule update --init --recursive
            mkdir build
            cd build
            cmake .. -GNinja
            ninja -j$(nproc)
